# 배포 환경 일관성을 위한 버전 고정
streamlit==1.30.0
openai==1.12.0
Pillow==10.2.0
pandas==2.2.0
numpy==1.26.4
requests==2.31.0

# AI/ML 라이브러리 (버전 고정)
torch==2.2.0
torchvision==0.17.0
ultralytics==8.1.0

# CLIP 모델 (안정 버전 - main 브랜치 사용)
git+https://github.com/openai/CLIP.git
ftfy==6.1.3
regex==2023.12.25

# LangChain (호환성 확보)
langchain==0.1.0
langchain-openai==0.0.5
langchain-community==0.0.13
langchain-core==0.1.10

# 컴퓨터 비전 (배포 환경 호환)
opencv-python-headless==4.9.0.80

# 데이터 처리 (버전 고정)
openpyxl==3.1.2

# 시각화 (안정 버전)
matplotlib==3.8.3
plotly==5.18.0

# 유틸리티 (버전 고정)
tqdm==4.66.2
python-dotenv==1.0.1
PyYAML==6.0.1
scikit-learn==1.4.0
reportlab==4.0.9 