# 새로운 기술 스택 - 핵심 패키지만
ultralytics>=8.0.0  # YOLOv8
openai>=1.0.0  # GPT-4 API
git+https://github.com/openai/CLIP.git  # CLIP model

# 기본 ML/AI 라이브러리
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.30.0

# LangChain (최신 패키지)
langchain>=0.1.0
langchain-openai>=0.1.0
langchain-community>=0.1.0
langchain-core>=0.1.0

# 컴퓨터 비전 (배포 환경 호환)
opencv-python-headless>=4.8.0
Pillow>=10.0.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# Streamlit 웹 프레임워크
streamlit>=1.28.0
streamlit-option-menu>=0.3.6

# 이미지 처리 및 시각화
matplotlib>=3.7.0
plotly>=5.15.0

# 유틸리티
tqdm>=4.65.0
python-dotenv>=1.0.0
requests>=2.31.0

# 모델 훈련 추가 패키지 (배포시 선택사항)
PyYAML>=6.0  # YOLO 설정 파일용
scikit-learn>=1.3.0  # 데이터 분할 및 평가

# CLIP 관련 (안정성 위해 추가)
ftfy>=6.1.0
regex>=2023.0.0

# PDF Report Generation (선택사항)
reportlab>=4.0.0 